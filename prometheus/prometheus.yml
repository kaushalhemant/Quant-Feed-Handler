global:
  scrape_interval: 2s
  evaluation_interval: 2s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "quantdesk-engine"
    static_configs:
      - targets: ["engine:9090"]
        labels:
          environment: "production"
          service: "quantdesk-feed-handler"
